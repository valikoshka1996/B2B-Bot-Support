# 🤖 B2B Support Bot

Telegram бот для підтримки клієнтів та адміністраторів у B2B-середовищі.  
Проєкт дозволяє швидко організувати процес обробки запитів клієнтів через Telegram,  
розподіляючи повідомлення між клієнтським ботом та адмінським ботом.

---

## 🧩 Основні можливості

- 👤 **Клієнтський бот** — приймає звернення від користувачів та надсилає їх адміністраторам.
- 🛠️ **Адмін-бот** — дозволяє адміністраторам переглядати нові повідомлення, відповідати клієнтам і керувати діалогами.
- 💾 **SQLite база даних** — зберігає повідомлення, клієнтів та історію комунікації.
- 🐳 **Docker підтримка** — повна ізоляція середовища через Docker Compose.
- ⚙️ **Гнучка конфігурація** через `.env`.

---

## 🏗️ Структура проєкту

```
.
├── .env                  # Конфігурація токенів та параметрів
├── docker-compose.yml    # Запуск контейнерів
├── Dockerfile            # Збірка Docker-образу
├── entrypoint.py         # Точка входу для запуску ботів
├── requirements.txt      # Python залежності
├── app/
│   ├── admin_bot.py      # Код адмінського бота
│   ├── client_bot.py     # Код клієнтського бота
│   ├── db.py             # Підключення та робота з базою даних
│   ├── models.py         # SQLAlchemy-моделі
│   └── utils.py          # Допоміжні функції
└── data/
    ├── support_bot.db    # SQLite база даних
    └── media/            # Збереження медіа-файлів
```

---

## ⚙️ Конфігурація `.env`

Перед запуском створіть файл `.env` у корені проєкту зі своїми параметрами:

```ini
# Telegram токени
CLIENT_BOT_TOKEN=your_client_bot_token
ADMIN_BOT_TOKEN=your_admin_bot_token

# Ідентифікатори адміністраторів (через кому)
ADMINS=123456789,987654321

# Налаштування бази даних
DATABASE_URL=sqlite:///data/support_bot.db
```

---

## 🚀 Запуск через Docker Compose

### 1️⃣ Побудова контейнера
```bash
docker-compose build
```

### 2️⃣ Запуск контейнера
```bash
docker-compose up -d
```

### 3️⃣ Перевірка логів
```bash
docker-compose logs -f
```

Після запуску обидва боти (клієнтський і адмінський) автоматично підключаться до Telegram і почнуть роботу.

---

## 🐍 Альтернативний запуск локально (без Docker)

### 1️⃣ Встановлення залежностей
```bash
pip install -r requirements.txt
```

### 2️⃣ Створення бази даних (якщо не існує)
```bash
python -m app.db
```

### 3️⃣ Запуск ботів
```bash
python entrypoint.py
```

---

## 💾 Збереження даних

- Всі дані (база даних, медіа) зберігаються у директорії `data/`
- При використанні Docker ці дані монтуються з локальної системи, тому не втрачаються при перезапуску контейнера.

---

## 🧠 Технології

| Компонент       | Технологія/Бібліотека |
|-----------------|------------------------|
| Мова            | Python 3.10+           |
| Боти            | aiogram                |
| База даних      | SQLite + SQLAlchemy    |
| Оркестрація     | Docker, Docker Compose |
| Середовище      | Linux / AlmaLinux      |

---

## 🔧 Основні команди Docker

| Команда | Опис |
|----------|------|
| `docker-compose up -d` | Запустити бота у фоні |
| `docker-compose logs -f` | Перегляд логів |
| `docker-compose down` | Зупинити контейнери |
| `docker system prune` | Очистити непотрібні образи та кеш |

---

## 🧑‍💻 Автор

Розроблено з ❤️ для ефективної взаємодії між клієнтами та адміністраторами.  
**B2B Support Bot** — гнучке рішення для вашого бізнесу.
